
$verbose = $true



#EMULATORS

#Azahar - 3DS 
	if ($verbose) { Write-Host "`n===== Setting up Azahar =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Azahar setup complete.`n" -ForegroundColor Green }

#BigPemu - Atari Jaguar
	if ($verbose) { Write-Host "`n===== Setting up BigPemu =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "BigPemu setup complete.`n" -ForegroundColor Green }

#Cemu - WiiU
	if ($verbose) { Write-Host "`n===== Setting up Cemu =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Cemu setup complete.`n" -ForegroundColor Green }

#Citra - 3DS
	if ($verbose) { Write-Host "`n===== Setting up Citra =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Citra setup complete.`n" -ForegroundColor Green }

#Citron - Switch	
	if ($verbose) { Write-Host "`n===== Setting up Citron =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Citron setup complete.`n" -ForegroundColor Green }

#Dolphin - Gamecube - Wii
	if ($verbose) { Write-Host "`n===== Setting up Dolphin =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Dolphin setup complete.`n" -ForegroundColor Green }

#Duckstation - PS1
	if ($verbose) { Write-Host "`n===== Setting up Duckstation =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Duckstation setup complete.`n" -ForegroundColor Green }

#Flycast - Dreamcast
	if ($verbose) { Write-Host "`n===== Setting up Flycast =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Flycast setup complete.`n" -ForegroundColor Green }

#lime3ds - 3DS
	if ($verbose) { Write-Host "`n===== Setting up lime3ds =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "lime3ds setup complete.`n" -ForegroundColor Green }

#m2emulator - Sega Model 2
	if ($verbose) { Write-Host "`n===== Setting up m2emulator =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "m2emulator setup complete.`n" -ForegroundColor Green }

#MAME
	if ($verbose) { Write-Host "`n===== Setting up MAME =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "MAME setup complete.`n" -ForegroundColor Green }

#MelonDS - DS
	if ($verbose) { Write-Host "`n===== Setting up MelonDS =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "MelonDS setup complete.`n" -ForegroundColor Green }

#mGBA - GBA
	if ($verbose) { Write-Host "`n===== Setting up mGBA =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "mGBA setup complete.`n" -ForegroundColor Green }

#PCSX2 - PS2
	if ($verbose) { Write-Host "`n===== Setting up PCSX2 =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "PCSX2 setup complete.`n" -ForegroundColor Green }

#Primehack - Wii Metroid
	if ($verbose) { Write-Host "`n===== Setting up Primehack =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Primehack setup complete.`n" -ForegroundColor Green }

#PPSSPP - PSP
	if ($verbose) { Write-Host "`n===== Setting up PPSSPP =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "PPSSPP setup complete.`n" -ForegroundColor Green }

#Retroarch - Multi system 
	if ($verbose) { Write-Host "`n===== Setting up Retroarch =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Retroarch setup complete.`n" -ForegroundColor Green }

#RPCS3 - PS3
	if ($verbose) { Write-Host "`n===== Setting up RPCS3 =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "RPCS3 setup complete.`n" -ForegroundColor Green }

#ryujinx - switch 
	if ($verbose) { Write-Host "`n===== Setting up ryujinx =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "ryujinx setup complete.`n" -ForegroundColor Green }

#Scummvm - Scummvm
	if ($verbose) { Write-Host "`n===== Setting up Scummvm =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Scummvm setup complete.`n" -ForegroundColor Green }

#Shadps4 - PS4
	if ($verbose) { Write-Host "`n===== Setting up Shadps4 =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Shadps4 setup complete.`n" -ForegroundColor Green }

#supermodel - SEGA Model 3 
	if ($verbose) { Write-Host "`n===== Setting up supermodel =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "supermodel setup complete.`n" -ForegroundColor Green }

#VITA3k - PSVita
	if ($verbose) { Write-Host "`n===== Setting up VITA3k =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "VITA3k setup complete.`n" -ForegroundColor Green }

#Xemu - Xbox
	if ($verbose) { Write-Host "`n===== Setting up Xemu =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Xemu setup complete.`n" -ForegroundColor Green }

#Xenia - Xbox 360 
	if ($verbose) { Write-Host "`n===== Setting up Xenia_canary =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "Xenia_canary setup complete.`n" -ForegroundColor Green }

#yuzu - Switch 
	if ($verbose) { Write-Host "`n===== Setting up yuzu =====" -ForegroundColor Magenta }



    if ($verbose) { Write-Host "yuzu setup complete.`n" -ForegroundColor Green }
